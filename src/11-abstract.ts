import { Animal, Cat } from './08-protected';

//const animal = new Animal('charlotte'); //The Abstract class only define the use in a child class
//animal.greeting();

const cat = new Cat('ichigo', 'Andrea');
cat.meow(2);
